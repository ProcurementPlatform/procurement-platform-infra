.terraform/
*.tfstate
*.tfstate.backup
*.tfplan
crash.log
crash.*.log
*.pem
.env
backend.hcl

# terraform-aws-modules/lambda build artifacts (zips regenerated on every apply)
builds/

# Local-only capstone prep docs — not tracked in the repo
docs/
